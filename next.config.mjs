/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    images: {
        unoptimized: true, // Useful for static export if needed, or if images are external
    },
};

export default nextConfig;
